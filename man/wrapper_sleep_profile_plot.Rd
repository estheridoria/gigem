% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/wrapper_sleep_profile_plot.R
\name{wrapper_sleep_profile_plot}
\alias{wrapper_sleep_profile_plot}
\title{Helper to Create and Save Population or Overlay Sleep Plot (Internal)}
\usage{
wrapper_sleep_profile_plot(
  filename,
  batchMeta,
  plot_data,
  divisions,
  numb_days,
  font,
  overlay_mode = FALSE,
  wrap_time = NULL
)
}
\arguments{
\item{filename}{A character string specifying the full path and filename for the output PDF.}

\item{batchMeta}{A data.table containing the metadata (behavr::meta(dt))
of the curated data, used for dynamic plot sizing.}

\item{plot_data}{A \code{behavr} table containing the final curated time-series data.}

\item{divisions}{A character vector (length >= 3) specifying the grouping columns
for colour, row facet, and column facet aesthetics, respectively.}

\item{numb_days}{Numeric, the number of days represented in the x-axis, used for width calculation.}

\item{font}{A character string for the font face (e.g., "plain", "bold").}

\item{overlay_mode}{Logical. If TRUE, the plot uses the condensed (overlay) facetting structure
(omitting the colour variable from the row facets). Defaults to FALSE (population mode).}

\item{wrap_time}{Time duration to wrap the x-axis (e.g., behavr::hours(24)). Defaults to \code{NULL}.}
}
\value{
\code{NULL}. Saves a PDF plot file.
}
\description{
This flexible internal function generates either a fully-faceted population plot
or a condensed overlay plot, saving the result as a PDF.
}
\keyword{internal}
